displays a list of module summaries

each module is a simple list [], no need for states

the modules are stored in a module dictionaries that has a key for the name and for the list

module summaries are names and number of items contained

modules are arranged in alphabetical order

modules can be deleted and edited

edited goes to separate editing screen, based on list screen

there is a new module button which also goes to the editing screen via a name entry screen